

import HomePage from '@/pages/home';

/**
 * トップページのコンポーネント
 * ホームページコンポーネントを返します。
 * @returns {JSX.Element} ホームページをレンダリングします。
 */
const IndexPage = () => {
  return <HomePage />;
};

export default IndexPage;
